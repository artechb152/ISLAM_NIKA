# דוח תיקונים — פרק 6, חמש מצוות היסוד

> **עדכון, סבב שלישי (15.7.2026, מאוחר יותר).** ועדת ביקורת של תשעה תפקידים עברה על הפרק, והתיקונים שאושרו בוצעו במלואם תחת הכלל שקבעת: אפס נגיעה בתוכן הכתוב. הרשימה המלאה — ממצאים, הכרעות, ביצוע, ביקורת חוזרת ו־QA סופי (54/54) — נמצאת ב‑**`CHAPTER6_COMMITTEE_REPORT.md`**, שהוא מעתה המסמך המוביל. צילומי לפני/אחרי: `_report/סקירה/` מול `_report/סקירה-3/`. הדוח שלמטה נשאר כתיעוד הסבבים הקודמים.

> **עדכון, סבב שני (15.7.2026).** מכאן ואילך האתר החי הוא אפליקציית **Next.js** תחת `web/`, ולא `design/`. כל הנתיבים בסעיפים 2–9 שלמטה מתארים את הגרסה שלפני ההמרה: `assets/chapter6/engine.js` הוא היום `web/src/lib/chapter6/engine.ts`, וכן הלאה. התוכן והעיצוב עברו זהים — אומת בהשוואת פיקסלים. הסעיף הבא מתאר את הסבב הזה; שאר הדוח נשאר כפי שהיה, כתיעוד של הסבב הראשון.

## 0. הסבב השני — המרה, ושתי מדידות שהפריכו את עצמן

### מה נבנה

הפרויקט הומר ל‑Next.js 16 עם React 19 ו‑TypeScript 5.9, לבקשתך, בלי לשנות תוכן או עיצוב. כל מסמך HTML הפך ל‑route group עם root layout משלו, מפני שמשתני ה‑`:root` של העמודים סותרים זה את זה בפועל — בכניסה `--maroon` הוא `#4a101e` ובשאר האתר `#841b1b` — ו‑layout משותף היה מאפשר לגיליון אחד לדרוס את השני. שלושת גיליונות ה‑CSS של הפרק הועתקו בייט‑בבייט. ארבעת המשתנים הגלובליים בוטלו: `window.CH6` הפך למודול `data.ts` שנוצר בסקריפט ואומת deep‑equal מול המקור, `window.CH6M` ל‑`mechanisms.ts` שקורא לרשמים באותו סדר שבו נטענו תגי ה‑`script` — כאשר `registerChecks` אחרון, כי הוא בעלים של מזהי התרגולים — ו‑`window.CH6E` ל‑`runtime.ts`. React מרנדר רק את המעטפת; `#chapter` ו‑`#rail-list` נשארים ריקים והמנוע ממשיך לבנות אותם אימפרטיבית, וזה מה שאיפשר להעביר כ‑3,700 שורות של קוד DOM מדוד בלי לגעת בו. `createEngine()` ו‑`initFilm()` מחזירים היום `destroy()`, מפני שאפקט של React נטען פעמיים ב‑StrictMode בעוד שתג `script` מעולם לא נדרש לנקות אחריו.

### שני באגים אמיתיים, ושניהם תוקנו

**הדיפ‑לינק היה שבור מזה זמן.** התנאי `/^#s\d/` דרש ספרה מיד אחרי ה‑s, והוא נכתב כשכל המסכים היו ממוספרים. אחרי ששמות המזהים שונו ל‑`sh-1`, `pr-v1`, `hj-c` וכדומה, נשארו רק `00` ו‑`01` פותחים בספרה — ו‑`00` הוא הסרטון עצמו, שהתנאי `deep > 0` בצדק מסרב לדלג אליו. כלומר מתוך 43 מסכים היה נגיש **אחד**, בעוד ההערה בקוד עדיין הבטיחה `#s07`, מזהה שכבר לא קיים. המזהה נלקח היום כמו שהוא והמפריד אופציונלי, כך ש‑`#s/sh-v` עובד וגם `#s01` הישן. נמדד: 43 מתוך 43 בשתי הצורות, ומזהה לא קיים או הסרטון נופלים כראוי לסרטון.

**תוכן נחתך בלי דרך להגיע אליו — הפגם החמור.** `.mech` היא העטיפה המשותפת של כל המנגנונים והיא `position:absolute;inset:0` בתוך `.stage-pane` שהוא `overflow:hidden`. ילד ממוקם‑אבסולוטית אינו תורם דבר לגובה הורהו, ולכן ה‑pane נשאר על ה‑min-height שלו בזמן שהתוכן גדל מעבר לו. התוצאה לא הייתה צפיפות אלא חיתוך: במדידה ברוחב 390 פיקסלים אבדו 176 פיקסלים במסך 01 ו‑404 ב‑`sum-end`, ובכללם טקסט מקור וחמשת כרטיסי המצוות. התיקון מבחין בין שני דברים. מסך 01 ומסך הסיום נמדדים לפי טקסט ולכן אינם יכולים להיות קנבס קבוע באף רוחב: ה‑pane שלהם הוא flex container והעטיפה `flex:1`, כך שהיא ממלאת את ה‑pane בדסקטופ בדיוק כמקודם, וה‑pane גדל אליה כשהתוכן גבוה יותר — פריט flex אינו מתכווץ מתחת לגובה התוכן שלו, וזו בדיוק הערובה שחסרה. שאר המנגנונים באמת קנבסים ונשארו כאלה עד 900 פיקסלים, שמתחתם הם נערמים וחוזרים לזרימה. בנוסף, `.s01-lead` — טקסט המקור של מסך 01 — ישב על 15 עד 16 פיקסלים והועלה ל‑19 עד 21, כמו `.read p`.

### שלוש מדידות שלי היו שגויות, וכדאי שיהיה לזה תיעוד

האבחון הראשון דיווח על חפיפות בתשעה מסכים; באמת יש שניים. הוא מדד חפיפה בזמן שהדף גלול למעלה, אבל תוכן שעובר מתחת לסרגל קבוע פשוט נגלל ומתפנה — השאלה האמיתית היא מה קורה בגלילה עד הסוף. הוא דיווח על שורות באורך 93 תווים; באמת 69 ב‑1920, בתוך הטווח שביקשת. הוא העריך אורך שורה בנוסחת רוחב חלקי חצי גודל הגופן, שהיא כלל אצבע ל‑ASCII ולא לגליפים עבריים; ספירת שורות מרונדרות בפועל דרך `Range` נותנת את המספר הנכון.

השלישית היא החשובה. האבחון דיווח על „containers ריקים" בחמישה מסכים, והבריף מבקש להסיר אותם. בבדיקה בפועל **כל אחד מהם צובע משהו**: `.s10-needle` הוא מחוג המצפן של שינוי הקיבלה, `.s13-ring-fill` היא טבעת ה‑2.5% של הצדקה, `.s07-sun` ו‑`.s07-shd` הם השמש והצל במחזור היום, `.track-line` הוא פס המסילה של הסליידרים, ו‑`.s01-detail` הוא פאנל ה‑aria-live שריק עד שלוחצים על מצווה. הם ריקים רק במובן שאין להם ילדים או טקסט — כל תוכנם הוא רקע, גבול או תמונה, וכך בדיוק בנויות ההמחשות. יישום הסעיף הזה כפשוטו היה מוחק את מחוג הקיבלה ואת טבעת הזכּאת. באותו אופן, שלושת הפיקסלים שדווחו כחיתוך ב‑`pr-v2` הם רעש עיגול תת‑פיקסלי על גובה שבור של 209.391 פיקסלים, והילד הגבוה ביותר במפה הוא 140 פיקסלים — כלומר שום דבר אינו נחתך שם.

### מה המדידה אומרת על הבריף

מתוך רשימת הבדיקות בבריף נמצאו שני פגמים אמיתיים, ושניהם תוקנו ואומתו. גלילה אופקית אין באף מסך ובאף רוחב. מסכים ריקים מדי אין — אף מסך אינו יורד מתחת ל‑45% מילוי בדסקטופ. כרטיסים מיותרים, containers ריקים ו‑overlaps רוחביים אינם קיימים בפרק. שש ההמחשות שהבריף מבקש „לבנות" — מחזור התפילה, שינוי הקיבלה, יום רמדאן, מסלול החג', הצדקה והשהאדה — כבר בנויות ועובדות, וההחלטה שלא להחליף אותן בתמונות מיוצרות התקבלה כבר בסבב הראשון ומתועדת בסעיף 7. הבריף כנראה נכתב מול גרסה מוקדמת יותר.

### מה אומת, ואיך

ההמרה נמדדה מול המקור ולא הוערכה: `/chapters` ו‑`/home` יצאו **אפס פיקסלים שונים** ב‑1920×1080, ב‑1366×768 ובמובייל; הליכה מסך אחר מסך בפרק 6 בשתי הגרסאות נתנה רצף זהה ואפס פיקסלים שונים במסך התרגול. אחרי תיקוני הסבב הזה, הליכה חוזרת ב‑1920 מראה שרק מסך 01 השתנה — 1.01% מהפיקסלים, וזה בדיוק שינוי הגופן שהבריף דורש — בעוד `sh-1`, `sh-2`, `sh-3`, `sh-v` ו‑`sh-c` נשארו זהים לחלוטין למקור. סימן ה‑RLM ‏(U+200F) בכותרת „פרקי הלמידה-" נשמר, ובלעדיו המקף עובר לצד הלא נכון. בידוד ה‑CSS אומת: `--maroon` הוא `#4a101e` בכניסה ו‑`#841b1b` בפרקים. תחת StrictMode שום דבר אינו נבנה פעמיים.

### שני אילוצי סביבה שנכפו על הבנייה

Turbopack אינו מסוגל לבנות מהתיקייה הזו: הוא חותך מחרוזת נתיב לפי אינדקס בייטים, החיתוך נופל באמצע האות א' בשם `אסלאם`, והתהליך קורס. זה באג שלו, ולכן הבנייה ושרת הפיתוח רצים על webpack. בנוסף, התקנת TypeScript מביאה היום גרסה 7 — הפורט ל‑Go — שאין לה נקודת כניסה ל‑JS API כלל, ו‑Next נשבר עליה בשגיאה חסרת פשר. הגרסה מקובעת ל‑5.9.3, ומכיוון שגרסה 7 גם פוסלת `baseUrl`, ה‑alias של `@/` מוגדר ב‑`next.config.ts`.

### מצב שתי התיקיות

חומרי המקור הועברו פנימה: `web/content/` מחזיקה היום את `chapter6-source.pdf` ואת `chapter6-source-content.pdf`, את הספֵּק המאושר `chapter6-experience-spec-final.json` ואת `opening page.png` — כולם אומתו בייט‑בבייט מול המקור. `design/` נשאר על מקומו לצד `web/` **בכוונה, ולא נמחק**; הוא גם אינו מנוהל ב‑git, ולכן מחיקתו הייתה סופית. הקובץ `chapter6-lesson.html` — לומדה ישנה שאף קוד לא הפנה אליה — נמחק לבקשתך, והגיבוי שלו נשאר ב‑`design/_backup/chapter6-lesson.html.bak`.

שווה לזכור שהאתר החי הוא `web/` בלבד. כל עוד שתי התיקיות קיימות, עריכה ב‑`design/` לא תשנה דבר במה שנשלח.

---

## 1. האבחנה

שלוש שורות CSS יצרו את רוב הבעיות שתיארת. ב-`lesson.css` הוגדר כרטיס התוכן כך:

```css
.content-pane{ max-height:clamp(320px,58vh,620px); overflow-y:auto; }
.content-pane p{ font-size:clamp(16px,1.02vw,17.5px); }
```

כלומר כל התוכן המקורי נדחס לתוך כרטיס בגובה קבוע עם גלילה פנימית משלו, בגופן שמגיע ל-17.5px לכל היותר, בעמודה צרה שיושבת לצד אינטראקציה גדולה. זו בדיוק הרשימה שכתבת: טקסט בכרטיס קטן, גלילה פנימית בתוך תיבה, ותוכן ארוך דחוס ליד אינטראקציה.

בנוסף, כל אחד מ-29 מסכי הפרק נשא מנגנון אינטראקטיבי משלו, וכפתור „המשך” היה נעול עד להשלמתו. משמע: הלומד נדרש לבצע פעולה בכל מסך כדי להתקדם, גם כשהפעולה רק המחישה טקסט שכבר היה מולו. זהו העומס.

---

## 2. הקבצים ששונו

כל קובץ גובה לפני השינוי אל `design/_backup/pre-fix-2026-07-15/`.

| קובץ | מה השתנה |
|---|---|
| `assets/chapter6/data.js` | נוצר מחדש בסקריפט. מבנה של 44 מסכים לפי ההנחיות, במקום 31 |
| `assets/chapter6/engine.js` | פריסה לפי סוג מסך; חסימה רק בבדיקות; תוויות כפתורים מהנתונים; פס התקדמות לפי מצוות; רמז אחרי טעות שנייה; תיקון פוקוס במגירה |
| `assets/chapter6/lesson.css` | עמודת קריאה אחת ב-19–21px; מסכי מעבר; קופסת רמז; סימון ✓ בפס ההתקדמות; תיקון ה-transition של המגירה |
| `assets/chapter6/mech.css` | עיצוב חמש הבדיקות ומפת החג'; מטרות מגע 44px |
| `assets/chapter6/mech-checks.js` | **חדש.** חמש בדיקות ההבנה, התרגול המסכם ומפת מסע החג' |
| `assets/chapter6/mech-opening.js` | „לקריאת הסיפור המלא”; הוסר כפתור מעבר כפול |
| `mech-shahada/prayer/charity/ramadan/hajj/summary.js` | חיבור מחדש של המנגנונים ששרדו; תיעוד מה פרש |
| `chapter6.html` | שורת `<script>` אחת; `tabindex` לגוף המגירה |

`film.js`, `film.css`, קבצי הווידאו והקול, `index.html`, `chapters.html`, `home.html` — לא נגעו.

---

## 3. מבנה הפרק אחרי התיקון

בכל מצווה הסדר קבוע: מסכי תוכן → המחשה → בדיקת הבנה אחת → מסך מעבר.

| מצווה | מסכי תוכן | המחשה | בדיקה | מעבר |
|---|---|---|---|---|
| פתיחה | סרטון + מסך המצוות | — | — | „מתחילים בשהאדה” |
| השהאדה | 3 | קליגרפיה | „מתי נאמרת השהאדה?” | „עוברים לתפילה” |
| התפילה | 5 | מחזור היום, שינוי הקיבלה | התאמת תפילה לזמן | „עוברים לצדקה” |
| הצדקה | 2 | טבעת 2.5% | 2.5% מ-10,000 | „עוברים לרמדאן” |
| צום רמדאן | 7 | יום הצום | סדר שלבי היום | „עוברים לחג'” |
| החג' | 7 | **מפת המסע (חדש)** | סדר התחנות | „לתרגול המסכם” |
| סיכום | — | — | התאמת סיטואציה למצווה | „סיום הפרק” |

**44 מסכים, 65 פסקאות מקור.**

### פיצול התוכן

הפסקאות לא הוקלדו מחדש. סקריפט העביר כל פסקה מהמבנה הישן למבנה החדש לפי הפניה, וסירב לכתוב את הקובץ אלא אם קבוצת הפסקאות זהה בייט-לבייט לפני ואחרי. הפלט: `65 source paragraphs, 0 lost, 0 altered`.

הפיצולים המשמעותיים — מסך אחד שנשא שש פסקאות (16, „זיכרונות בתוך הירח”) פוצל לשלושה מסכי נושא; מסך אחד שנשא ארבע (23) פוצל; הטוואף והסעי, שחלקו מסך, קיבלו מסך כל אחד. שום פסקה לא קוצרה, לא סוכמה ולא הושמטה.

---

## 4. תרגולים שהוסתרו

נשארו שש בדיקות בלבד — אחת לכל מצווה ואחת מסכמת, כפי שביקשת. **שום קוד לא נמחק.** 23 המנגנונים שאינם בזרימה רשומים תחת מזהי מסך שכבר לא קיימים ב-`data.js`, ולכן המנוע לא מגיע אליהם לעולם. אין אליהם קישור ואין כפתור. הוספת מסך עם המזהה שלהם מחזירה אותם.

| קובץ | פרשו |
|---|---|
| `mech-shahada.js` | 03, 04, 05 |
| `mech-prayer.js` | 06, 08, 09, 11, 12 |
| `mech-charity.js` | 14, 15 |
| `mech-ramadan.js` | 16, 17, 19, 20, 21, 22 |
| `mech-hajj.js` | 23–28 (כל הקובץ) |
| `mech-summary.js` | 29 |

הטקסט שהמנגנונים האלה נשאו לא אבד — הוא זה שמוצג עכשיו כמסכי קריאה.

חמש הבדיקות שביקשת נכתבו מחדש ב-`mech-checks.js` ולא הותאמו למנגנונים הישנים, מפני שהן שונות מהם בפועל: לבדיקת השהאדה ביקשת חמש אפשרויות (הקיימת נשאה ארבע), לצדקה ביקשת ארבע אפשרויות בחירה (הקיימת הייתה מחוון רציף), ולרמדאן ביקשת חמישה שלבים אחרים. הנוסח, האפשרויות והמשובים נקראים מ-`data.js` — הם לא כתובים בקוד.

---

## 5. באגים שתוקנו

1. **הטקסט נלכד בכרטיס עם גלילה פנימית.** תוקן: מסך תוכן הוא עמודה אחת ברוחב 72 תווים, בגלילת העמוד הרגילה. נמדד על 43 מסכים: `text trapped in a scrolling box: none`.
2. **גופן הגוף הגיע ל-17.5px.** תוקן ל-19–21px בדסקטופ, 19px במובייל. נמדד: `content screens under 19px: none`.
3. **כל מסך חסם את ההמשך.** תוקן: המנוע מסמן כל מסך כעביר עם הצגתו, ורק `kind:'check'` חוסם. ההמחשה כבר לא יכולה להפוך לשער. נמדד: שש הבדיקות בלבד חוסמות.
4. **המגירה של הסיפור המלא נפתחה בלי שהפוקוס נכנס אליה.** באג נגישות אמיתי: `.drawer` הוגדר עם `transition: … visibility .42s`, ומעבר `hidden→visible` מחזיק את ערך ההתחלה ב-t=0 — כלומר הפאנל עדיין `visibility:hidden` באותו טיק שבו הוא נפתח, ו-`focus()` על אלמנט מוסתר הוא no-op שקט. הדיאלוג נפתח והמקלדת נשארה בדף מאחוריו. `requestAnimationFrame` אינו הפתרון — הקריאות שלו רצות לפני חישוב הסגנון של אותו פריים. התיקון: לא לעשות transition ל-`visibility` בכיוון הפתיחה. נמדד: `focusIsInsideDialog: true`, ו-Escape מחזיר את הפוקוס לכפתור שפתח.
5. **מטרות מגע קטנות מ-44px.** `.mech-btn` נמדד 39px, `.s01-story` נמדד 35px. שניהם קיבלו `min-height:44px`. נמדד על כל הפרק ב-1920 וב-375: אין פקד מתחת ל-44 באף ממד.
6. **שני כפתורי „מתחילים בשהאדה” במסך 01** — אחד של המסך ואחד של סרגל הניווט. הוסר של המסך.
7. **פס ההתקדמות אמר „מסך 12 מתוך 44”** — מספר שלא ניתן לפעול לפיו. עכשיו: „השהאדה · 2 מתוך 6”.

**באג שה-QA תפס בקוד החדש שלי, לפני מסירה:** בגרסה הראשונה של בדיקת הסדר, טעות בשלב הראשון החזירה את *כל* השלבים לבנק — כלומר איפוס מלא, בדיוק מה שאסרת. תוקן לתיקון מקומי אמיתי: כל שלב שנמצא במקומו ננעל שם גם אם שכניו שגויים, ורק מה שלא במקומו חוזר. נמדד בסדר הפוך מלא: שלב אחד ננעל, בניסיון השני שלושה.

---

## 6. הבדיקות שבוצעו

הכול נמדד בדפדפן אמיתי דרך CDP, לא בהערכה.

**מעבר על כל הפרק (43 מסכים):** אין פסקה חסרה, אין טקסט בתיבה נגללת, אין מסך עם יותר ממסך פעיל אחד, אין גלילה אופקית, אין שגיאות Console, אין קבצים חסרים.

**חמש הבדיקות + התרגול המסכם:** כל אחת הופעלה — טעות, טעות שנייה, ואז פתרון. הרמז מופיע בדיוק אחרי הטעות השנייה ולא לפניה. טעות אף פעם לא מאפסת: בהתאמת התפילות ההתאמות שנעשו נשארות, בבדיקות הסדר השלבים הנכונים ננעלים.

**תצוגות:** 1920×1080, 1366×768, 820 (טאבלט), 375 (מובייל צר) — אין גלילה אופקית באף אחת, אין טקסט מתחת למינימום, אין פקד מתחת ל-44px. סרגל הניווט התחתון לא מסתיר תוכן: במובייל צר, בגלילה לתחתית המסך הארוך ביותר, השורה האחרונה מפנה 29px מעליו.

**המגירה:** נפתחת עם שש פסקאות הסיפור, הפוקוס נכנס, יש שני עוגני Tab בפנים, Escape סוגר ומחזיר את הפוקוס לכפתור.

**הסרטון:** נבדק עם קלט עכבר אמיתי (לא `click()` תוכנתי, שאינו נחשב אישור משתמש ולכן לא מפעיל קול). נח על הפריים הראשון; לחיצה מפעילה עם קול מהשנייה הראשונה; כפתור השהיה עובד; כפתור השתקה עובד; דילוג מציג אזהרה ואז נוחת על מסך 01 עם הכפתור „מתחילים בשהאדה”; גרירת הטיימליין ל-0:52 ממשיכה לנגן.

**ניווט:** חזרה והמשך עובדים; פס ההתקדמות מציג את חמש המצוות בשמן, עם הנוכחית מודגשת, מצוות עתידיות נעולות, ו-✓ על מצווה שהושלמה (סימן, לא צבע בלבד).

---

## 7. החלטה שכדאי שתדעי עליה

ביקשת לפחות ויזואל מרכזי אחד לכל מצווה, ונתת שמות קבצים ופרומפטים. ארבע מתוך חמש ההמחשות שתיארת **כבר קיימות בפרויקט** — לא כתמונות אלא כרכיבים אינטראקטיביים: מחזור היום עם חמש התפילות, מפת שינוי הקיבלה מירושלים למכה, טבעת 2.5% שזורמת לארבעת מקבלי הצדקה, ויום הצום מעלות השחר עד התראויח. הן עונות בדיוק על ה-Alt שכתבת.

לכן לא ייצרתי תמונות סטטיות במקומן: הן היו מתחרות בהמחשה שכבר עובדת, נכנסות תחת „אל תעמיס תמונות דקורטיביות” ו„אל תייצר מחדש assets קיימים”, וצורכות קרדיטים. מה שכן היה חסר — מפת מסע החג' — **נבנה**, באותו סגנון, עם שבע התחנות לפי סדרן.

`MAGNIFIC_IMAGE_PROMPTS.md` סופק כפי שביקשת, עם פרומפט מלא לכל אחת מהשש. אם תרצי גרסה מצוירת במקום או בנוסף — זו הרצה אחת ואני עושה את זה.

---

## 8. מה נשאר פתוח

1. **שמירת ההתקדמות עדיין כבויה — במכוון, ולבקשתך גם בסבב השני.** הדגל הוא `PERSIST = false` בראש `web/src/lib/chapter6/engine.ts` (היה `engine.js`). לכן שני סעיפים ברשימת ה-QA שלך — „שמירת מצב המסך הנוכחי” ו„רענון הדפדפן” — מתנהגים כמתוכנן־זמנית: רענון מתחיל את הפרק מחדש ומריץ את הסרטון. זו עבודה לא גמורה שצריך להחזיר, לא החלטת עיצוב. שינוי דגל אחד מחזיר את זה; שום דבר אחר לא צריך להשתנות.
2. **`design/chapter6-lesson.html` — נמחק** בסבב השני, לאחר אישורך. הגיבוי נשאר ב-`design/_backup/chapter6-lesson.html.bak`.
3. **מסכי תוכן קצרים** — `sh-1` ו-`ch-1` נושאים משפט מקור אחד כל אחד, כי כך חילקת אותם. במסך מלא הם נראים דלילים. אם תרצי, אפשר לאחד כל אחד מהם עם המסך שאחריו בלי לפגוע בתוכן.
4. **`:has()` ב-CSS** — רוחב כרטיס הבדיקה משתמש בו. בדפדפן ישן שלא תומך, הכרטיס פשוט יהיה רחב יותר; שום דבר לא נשבר.

---

## 9. צילומי מסך

> **מעודכן בסבב השני.** כל הצילומים כאן צולמו מחדש מאפליקציית ה‑Next.js הנוכחית, ב‑1600×900 בצפיפות כפולה. הסט הקודם היה מלפני ההמרה ומלפני שני התיקונים, ולכן כבר לא הראה את מה שנשלח; הוא הוסר כדי שלא יישארו שני קבצים באותו שם שמראים דברים שונים. המספור השתנה בהתאם.

ב‑`_report/` יש היום חמישה־עשר צילומי דסקטופ, לפי סדר הפרק: `01-opening` הוא מסך „מחמש המצוות לחמש פעולות"; `02-content-shahada` הוא מסך תוכן, כלומר עמודת הקריאה; `03-visual-shahada` היא המחשת הקליגרפיה; `04-check-shahada` הוא תרגול השהאדה; `05-transition-shahada` הוא מסך מעבר. אחריהם `06-visual-prayer-times` — מחזור היום ותפילותיו; `07-visual-prayer-qibla` — שינוי כיוון התפילה; `08-check-prayer-match` — התאמת תפילה לזמן; `09-content-prayer-long` — מסך תוכן ארוך בן שש פסקאות. ואז `10-visual-charity-ring` — טבעת ה‑2.5%; `11-visual-ramadan-day` — יום הצום מעלות השחר עד התראויח; `12-hajj-route-map` — מפת מסע החג'; `13-check-hajj-order` — סדר התחנות; `14-final-exercise` — התרגול המסכם; ו‑`15-finish` — סיום הפרק.

חמישה צילומי מובייל נוספו ב‑390×844, ושניים מהם הם הראיה לתיקון החיתוך: `mobile-01-opening` ו‑`mobile-02-finish` הם בדיוק שני המסכים שבהם אבדו 176 ו‑404 פיקסלים, וכעת הם שלמים. לצידם `mobile-03-content` (מסך קריאה), `mobile-04-check` (תרגול) ו‑`mobile-05-prayer-cycle` (מחזור התפילות).

בתיקייה `_report/המחשות/` יש בנוסף את שש ההמחשות בלבד, בקבצים בשמות עבריים, לצפייה מרוכזת: הקליגרפיה, מחזור היום, שינוי הקיבלה, טבעת הצדקה, יום הרמדאן ומסלול החג'.
