# גרסה ישנה — לא האתר החי

התיקייה הזו היא הגרסה המקורית של האתר (HTML/CSS/JS ללא framework), שנשמרה כרפרנס אחרי ההמרה ל‑Next.js ב‑15.7.2026.

**האתר החי הוא `../web/`. עריכה כאן לא תשנה שום דבר במה שרץ בפועל.**

חומרי המקור (ה‑PDF של התוכן, הספֵּק המאושר, `opening page.png`) הועתקו ל‑`../web/content/`.
