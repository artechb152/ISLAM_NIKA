# האסלאם — דת ותרבות

אתר למידה בעברית (RTL), Next.js, `web/`. כל פרק הוא מאמר עם מנגנונים.

---

## ⛔ לפני כל עבודה על פרק — חובה

**קרא/י את [`concept/BUILD-RULES.md`](concept/BUILD-RULES.md) במלואו.**

זה לא מסמך רקע. אלה 76 כללים מחייבים ב-12 חלקים — אפיון, פרוטוקול העבודה עם
המשתמשת, נאמנות למקור, מערכת העיצוב עם הערכים עצמם, צבע, תמונות, פריסה,
מנגנונים, ומלכודות טכניות שכל אחת מהן נצרבה מבאג אמיתי שעלה שעות.

**אין להציע מבנה לפרק, לייצר נכס, או לכתוב קומפוננטה לפני קריאתו.**

הכללים הנשברים בתדירות הגבוהה ביותר, וכולם עלו ביוקר:

- **אין לבן בשום מקום.** המבחן הוא נייטרליות, לא בהירות. כרטיסייה היא `--paper`.
- **כל מחרוזת לימודית נושאת §N מ-SOURCE-TEXT.** הקומפוננטה לא כותבת משפט.
- **אין להסיק ולהציג כעובדה** — ספירה או מסקנה שאינה במקור לא עולה למסך.
- **מודדים מול פרק 6 לפני שמתקנים.** לא מנחשים ולא זוכרים.
- **`inset-inline-start` במאמר RTL ממראה מפה** — לוח גאוגרפי הוא `dir="ltr"`.
- **פרק 4 מחוץ לתמונה** — הוא לא גמור. הבסיס הוא פרק 6 ופרק 2.

---

## מפת הפרויקט

| נתיב | מה |
|---|---|
| `concept/BUILD-RULES.md` | **הכללים. להתחיל כאן.** |
| `concept/chapterN/SOURCE-TEXT.md` | טקסט המקור, מילה במילה, מפוצל ל-§N |
| `concept/chapterN/DECISIONS.md` | הכרעות הבנייה של אותו פרק, עם הסיבות |
| `concept/chapterN/verify-chapterN.mjs` | שער נאמנות · כיסוי · פעם אחת בדיוק |
| `web/src/lib/chapterN/passages.json` | כל מילה בפרק, פעם אחת |
| `web/src/lib/chapterN/layout.json` | אילו קטעים בכל מקטע ובאיזה תפקיד |
| `web/src/styles/chapter6-article.css` | **מערכת העיצוב.** כל פרק טוען אותה ראשונה |

## הרצה ובדיקה

```bash
export PATH="$HOME/.local/node/bin:$PATH"     # node אינו ב-PATH כברירת מחדל
cd web && npm run dev                          # localhost:3000
node ../concept/chapter5/verify-chapter5.mjs   # שער הפרק
npx tsc --noEmit                               # typecheck
```

צילומי מסך: Playwright מול `/Applications/Google Chrome.app`, כמו בסקריפטים
שב-`web/scripts/`. **שער ירוק אינו עדות שהעמוד נראה נכון — לצלם.**

## מצב הפרקים

| פרק | מצב |
|---|---|
| 1 · ההיסטוריה של חצי האי ערב | עולם תלת-ממדי, בעבודה |
| 2 · תרבות שבטית | ✅ מאמר גמור — מקור כללי המאמר |
| 3 · ראשית חיי מוחמד | קומיקס |
| 4 · ההג׳רה והקרבות | ⚠ **לא גמור. לא להסתמך עליו ולא להעתיק ממנו.** |
| 5 · שאלת הירושה והח׳ליפים | ✅ גמור |
| 6 · חמש מצוות היסוד | ✅ **אמת המידה לעיצוב ולמנגנונים** |
